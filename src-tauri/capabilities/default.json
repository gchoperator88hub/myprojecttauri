{
  "$schema": "../bindings/capabilities.json",
  "identifier": "default",
  "description": "Main application capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:allow-version"
  ]
}